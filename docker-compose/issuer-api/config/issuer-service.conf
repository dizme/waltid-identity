# Pubblico per OID4VCI (EUDI app richiede https su credential_offer_uri / credential_issuer).
# Terminazione TLS su Caddy (porta ISSUER_API_TLS_PORT); upstream issuer-api resta HTTP in rete Docker.
baseUrl = "https://${SERVICE_HOST}:${ISSUER_API_TLS_PORT}"
